setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  command:
    - bash ./startup.sh
  image: web
run:
  web: gunicorn maplocate_heroku.wsgi
